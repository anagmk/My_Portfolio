import React from "react";
import HeroText from "../components//HeroText.jsx";

const Hero = () => {
  return (
    <section id="hero" className="min-h-screen overflow-hidden hero">
      <HeroText />
    </section>
  );
};

export default Hero;
